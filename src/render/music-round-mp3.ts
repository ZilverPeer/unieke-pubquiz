import type { QuizContent } from "@/domain";

/** Renders the Music Round MP3 (slot 7) with the Locale's Announcements. Implemented in ticket #10. */
export async function renderMusicRoundMp3(_quiz: QuizContent): Promise<Buffer> {
  throw new Error("renderMusicRoundMp3 is implemented in ticket #10");
}
