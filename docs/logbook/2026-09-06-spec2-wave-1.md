# Logbook: spec 2 wave 1, shop and schema (2026-09-06)

Purpose: a factual record of how each orchestration session went, so the process can be evaluated. Facts per ticket; judgement under "Observations".

## Session summary

- Spec #36 (order-to-delivery pipeline, local full loop) written after grilling; Erik's decisions: full loop including wp-env, links valid 30 days, local only, no content import. Seven tickets #37-#43 published.
- Erik's process feedback: skip questions he would answer with the recommendation; ask only decisions that matter, one at a time, minimal wording. Retro note on wave 5: the property tests were worth it; the seed extension and the second reviewer on a ten-line fix were the cost.
- Pinned on master before dispatch (9dd2b71): `pg-boss`, `pg`, `@types/pg`; `src/domain/orders.ts` (Order and Quiz records, `QuizStatus` with legal transitions, Deliverable file names); `src/domain/checkout.ts` (meta_data keys, operator note prefix); `src/deliver/index.ts` interface stub and README.
- Stack started and seeded once for #38. #37 uses Docker for wp-env, not the Supabase stack.
- Dispatched 19:47, both in one message. #40 dispatched 20:22 after #38 merged; #42 dispatched 21:14 after #40 merged.

## Per ticket

### #38 Orders and quizzes schema, repository (PR #44) — dispatched 19:47, merged 20:19 (32 minutes)

- Implementer: one commit (8a7529c), 14 minutes. Migration 00008 (orders, quizzes, quiz_status enum, deliverables bucket, RLS, moddatetime), `createOrderRepository` as a sibling of `createRepository`, 21 integration tests. Red evidence: import error, then `categoryPicks` round-trip (jsonb null vs undefined), converted on every read path. Interface note: jsonb cannot hold undefined; the webhook parser (#39) must expect null.
- Standards review: HARD, the Composition-delete restriction was claimed in the migration comment and the acceptance criteria but never tested. A second HARD (integration count 36 vs 32 `it` blocks) was wrong: the count includes the generate suite. JUDGEMENT: raw_payload test asserted only wooStatus; lost compare-and-swap races surfaced as a generic no-rows error; `normalizeBillingEmail` duplicated from compositions.
- Spec review: typecheck ok, unit 88/88, eslint ok, integration 36/36; types regeneration clean; both FK restrictions proven with psql; two concurrent upserts of the same order yielded one order and two Quizzes; RLS on, bucket private. All criteria PASS.
- Fix round (92ccc6e): red-first Composition-delete test (`expected null not to be null` after temporarily making the FK cascade), raw_payload asserted directly, `QuizStatusChangedConcurrentlyError` for lost races (untested: a real race cannot be forced without mocking). Integration 37/37.
- Merged; master checks green; worktree removed.

### #40 pg-boss worker (PR #45) — dispatched 20:22, merged 21:12 (50 minutes)

- Implementer: one commit (aba04a5), 25 minutes. `src/worker/{boss,quiz-job,sweep,index}.ts`, `src/instrumentation.ts` gated on `PUBQUIZ_WORKER=1` and the Node runtime, `uploadToDeliverablesBucket`, six integration tests against the real engine. Interface gap: `QuizRecord` carries no billing email, so `getOrderById` was added to the repository. Out of scope: no reaper for a Quiz left `generating` after a crash; `deliverQuiz` failing every retry leaves the order incomplete (deferred to #43).
- Standards review: HARD, the lookup, the `generating` transition and the order lookup sat outside the try, so an error there on the last attempt dead-lettered the job without `failed` or a note. HARD, the download URL shape `/download/<token>/<file>` was invented by the worker; spec #36 said `/download/<token>` and #42 owns the route. JUDGEMENT: partial uploads on a final failure not documented; red evidence only for test plumbing, not for the retry logic.
- Spec review: typecheck ok, unit 88/88, eslint ok, integration 43/43; red evidence proven by deleting the handler; live run under `next dev` swept one pending Quiz and stopped cleanly on CTRL_C. Live delivery cannot complete until #41 because `createDeliverer` throws. All six criteria PASS. Noted that `src/scripts/generate-quiz.ts` already imports both `sample` and `render`, so the worker is not the only module crossing that boundary.
- Fix round (41eefb9): whole pre-delivery section in one try, `failQuiz` re-reads after a lost compare-and-swap, stale-`generating` test with `retryLimit 0` ends `failed` with a note; `downloadPath(token, file)` pinned in `src/domain/orders.ts` with a unit test; spec #36 and ticket #42 commented with the shape. Unit 90/90, integration 44/44.
- Merged; master checks green; worktree removed.

## Friction log

- The Spec reviewer for #45 saw the `quizzes` table change under it (rows appearing and disappearing) while the #40 implementer ran integration tests on the shared stack. Same class as the #44 friction; the worker's suite cleans up after itself, so no wrong verdict this time.
- The #37 implementer stopped its turn to "wait for" a background `wp-env start` instead of polling, and sat idle until nudged at 20:58. Briefs should say: never end the turn to wait for a background command.

- The #44 fix round ran while the Spec reviewer was still using the shared stack; the reviewer saw one transient FK misread and one flaky generate integration assertion, both explained by the implementer's concurrent db:reset. Fix rounds that reset the stack should wait for the Spec review to finish, or the Spec reviewer should be told a reset may happen.

## Observations (judgement)
