# Logbook: spec 2 wave 1, shop and schema (2026-09-06)

Purpose: a factual record of how each orchestration session went, so the process can be evaluated. Facts per ticket; judgement under "Observations".

## Session summary

- Spec #36 (order-to-delivery pipeline, local full loop) written after grilling; Erik's decisions: full loop including wp-env, links valid 30 days, local only, no content import. Seven tickets #37-#43 published.
- Erik's process feedback: skip questions he would answer with the recommendation; ask only decisions that matter, one at a time, minimal wording. Retro note on wave 5: the property tests were worth it; the seed extension and the second reviewer on a ten-line fix were the cost.
- Pinned on master before dispatch (9dd2b71): `pg-boss`, `pg`, `@types/pg`; `src/domain/orders.ts` (Order and Quiz records, `QuizStatus` with legal transitions, Deliverable file names); `src/domain/checkout.ts` (meta_data keys, operator note prefix); `src/deliver/index.ts` interface stub and README.
- Stack started and seeded once for #38. #37 uses Docker for wp-env, not the Supabase stack.
- Dispatched 19:47, both in one message.

## Per ticket

## Friction log

## Observations (judgement)
