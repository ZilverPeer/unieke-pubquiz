# Spec 3b wave 1 (2026-09-08)

Spec #69: one Category picker with cycled rounds, one zip per quiz, bare chrome. Written after Erik's first browser walkthrough of spec #55 (comments: eight dropdowns, duplicates rejected after payment, four picks should give two rounds each, four loose files per quiz, download not on first click, redundant Storefront chrome, 4 s per page).

Tickets, serial in one worktree `../Pubquiz-wt-3b` (the old `../Pubquiz-wt-shop` folder is an empty directory still held open by a finished reviewer shell; a new path means one fresh wp-env instance for the wave): #70 chrome + front page (folds #68), #71 cycle rule + Quiz mode removed, #72 three-field product page (blocked by #71), #73 one zip per Quiz, #74 walkthrough + #67 + #66 (blocked by all).

Stack: orchestrator started the Supabase stack before dispatch, seed intact (8 Categories, 2640 Items, no reset needed).

## Session summary

(filled at wave end)

## Tickets

### #70 bare chrome and visible front page

Dispatched 11:20 to a Sonnet implementer in `../Pubquiz-wt-3b` (fresh wp-env instance, first `shop:up` 6-10 min). PR #75 open 11:57 (26 min; first `shop:up` took 5 min). Interface gap found empirically: Storefront's `is_active_sidebar('sidebar-1')` is true on a fresh instance because WordPress parks its default widgets there, so the plugin forces `storefront-full-width-content` itself.

Implementer reported `src/scripts/generate-quiz.test.ts` as a "known flake" (201/202). Orchestrator root-caused it on master: both tests render four real Deliverables (about 4 s each alone) under vitest's default 5 s timeout, and fail whenever the suite runs next to the wp-env and Supabase containers; the music renderer tests already carry 60 s. Fixed on master (b298d02), suite 198/198 again.

Standards review 12:03: HARD `setup-shop.php` trashes post 1 on existence alone (no identity check); JUDGEMENT option write order (`show_on_front` before `page_on_front` leaves a blank front page on partial failure); JUDGEMENT "Mijn account" literal duplicates the page title; JUDGEMENT the hook-name pins are a drift guard, not behavioural proof. HARD sent to the implementer immediately, Spec review pending.

Fix round 1 pushed 12:16 (identity guard with a new pin, option order, page title read from the page). Spec review 12:18: all six checks PASS, no findings; integration 74/74 on the branch; its two `npm test` runs showed the generate-quiz timeouts (the branch predates the master fix). Merged 12:22 (PR #75, issues #70 and #68 closed). Wall clock 62 min from dispatch.

### #71 cycle rule, Quiz mode removed

Dispatched 12:12 in parallel to #70's review, in a second worktree `../Pubquiz-wt-71` without a shop instance (the ticket is code, schema and docs; the two instance-bound checks, fixture recapture and the 4-pick loop order, move to #72 where they happen anyway). Deviation from "shop tickets share one worktree, one at a time": this ticket does not touch the shop setup. The migration is applied by the orchestrator with `db:reset` after #70's Spec review has finished with the stack.

PR #76 open 12:39 (27 min). Reviews dispatched 12:41: Standards, and Spec part 1 (unit, property bite tests, grep, docs, migration text). Spec part 2 (generate CLI runs, integration suite) waits for a `db:reset` with the new migration, which cannot happen while #73 verifies its loop on the old schema.

Standards review 12:46: HARD no red evidence in the PR body (it was in the report to the orchestrator only); HARD a stale "single_category mode" comment in `src/sample/index.ts`; JUDGEMENT no test that a present `pubquiz_mode` key is ignored. Spec part 1 12:48: all six checks PASS, mutation tests bite (off-by-one and duplicate guard), master's parser fails the new tests; the grep criterion is not literally clean (eight comment/doc hits). One fix round bundling both; pushed 12:56 (4601b79: comments reworded, red evidence in the PR body, a parser test that a present `pubquiz_mode` key is ignored). Unit 209/209. Waiting for #73 to release the stack before the `db:reset` and Spec part 2.

Merge round after #77 landed: pushed 14:21 (617142a), two real conflicts (worker imports, route integration test imports), migration renamed `00011_drop_quiz_mode.sql`, unit 218/218.

14:29 the orchestrator found every container gone: Supabase, the wp-env instance, Mailpit and the ticker (the last two "Exited 5 minutes ago"), the shape of an `npm run loop:down`. No agent was running a stop (the #73 implementer had finished at 14:17 reporting the shop and stack up; the #71 implementer never touches them); most likely Erik's own window. Recovered: `supabase start` plus `db:reset` from the #71 worktree (schema now without `quiz_mode`, bucket MIME types pdf/mpeg/zip, seed 8/2640), Spec part 2 sent 14:34. The shop instance restarts under #72 from `../Pubquiz-wt-3b` (recreated on `ticket-72-product-page`).

Spec part 2 14:37: all four generate CLI scenarios PASS (4 picks give Categories 1,2,3,4,1,2,3,4 over the slots; 1 pick fills all 8; 0 picks 8 distinct; duplicate pick exits 1 and persists nothing); integration suite 71/72. The one failure was on master already: PR #77 left `quiz-job.integration.test.ts` expecting four loose objects where a Quiz now uploads one zip, and #77's Spec review had run unit only. Fixed on master by the orchestrator (a7a5ffc, verified 7/7 after the merge). Merged 14:40 (PR #76, #71 closed). Master: typecheck, unit 218/218, eslint, worker integration 7/7. Wall clock 148 min from dispatch, of which about 80 min waiting for the stack (the #73 loop, then the reset).

### #72 three-field product page

Dispatched 14:52 in `../Pubquiz-wt-3b`, recreated on `ticket-72-product-page` from master 46a7e36 (contains #71 and #73). The wp-env instance's containers were removed at 14:25; the implementer's first `shop:up` restarts it from the surviving volumes.

PR #78 opened 15:28 after a full `shop:up` and an empirical three-pick order (fixture recaptured; `_wapf_meta` `categories.raw` is an array of Category id slugs in choice order, an unfilled field yields no entry). Spec review (own worker in the review clone; its worker consumed one test job, a false negative the reviewer traced and re-ran at 5/5) and Standards review found four JUDGEMENT items, batched into one fix round (cap pinned to the slot count instead of a free `8`, required-flag assertion, "dropdown" wording, `pricing_type => 'none'` on the choices; the "(+€0.00)" price hint and PHP warnings came from our own field-group definition, not the plugin). Fix b858b00 clean. PR #79 landed meanwhile and both touched the walkthrough's curl block; the implementer merged master (e872c09, one prose conflict). Merged 15:49 as be2e887, #72 closed, master green (typecheck, unit 228/228, eslint).

### #66 + #67 loop output (code half of #74)

Erik asked at 15:20 whether the orchestrator's idle time could be used by running more agents in parallel. Answer: the wave is bound to one stack and one shop instance, so shop tickets stay serial, but #74's two code changes (#66 no keys in `loop:up` output, #67 product id lookup) need no shop. Split out and dispatched 15:25 in `../Pubquiz-wt-66` (branch `ticket-66-67-loop-output`, code-only, unit tests at two pure seams, forbidden to touch the loop). #74 shrinks to the walkthrough rewrite and the fresh-loop proof; its brief updated.

PR #79 opened 15:33. Standards review: two HARDs (a dead `JSON.parse` with an unused parameter in the summary function, no red evidence in the PR body), fixed in 3a5fb66. Merged 15:40 as a914d0d, #66 and #67 closed. The empirical fresh-loop proof (no keys printed, `Product: #<id>` from `.local/shop-setup.json`) is deferred to #74's Spec review.

### #74 walkthrough on a fresh loop

Dispatched 15:59 in `../Pubquiz-wt-3b`, switched to `ticket-74-walkthrough` from master b0cc243 (same directory, so the wp-env instance is reused). Sole agent on the machine, so the brief granted a one-ticket exception to run `loop:down` and `loop:up` (never `db:reset`, never `wp-env destroy`). PR #81 opened 16:19 after a fresh loop, order 33 with three picks (one zip, four Deliverables, Mijn account row) and the failing order. The product-page and zip sections had already been rewritten by #72 and #79, so the diff is the completed-mail and admin-mail subjects the fresh loop disproved, plus the `<site name>` convention. Asked at 16:22 to also fix the runbook's stale English mail subjects (in scope: runbook consistency) before the reviews start.
Fixed in 8f1b060. Reviews dispatched 16:21. Standards: no HARD, one JUDGEMENT (PR body without the report header and commit hashes). Spec review (fresh `loop:down`/`loop:up`, key-scan count 0, orders 35 to 37: three-pick order with one zip and four Deliverables, account order with one Mijn account row, failing order with the alert, 9-pick cap refused): two HARDs in files the PR did not touch but the brief put in scope (runbook download example saves as `quiz.zip`; README still names "Soort quiz"). All three batched into fix round 1 at 16:35.
Fix 1e2c8f9 (runbook download name per Quiz, README without the old field name, PR header). Merged 16:41 as 8f2856d, #74 closed. Master `npm run check` green (typecheck, unit 228/228, eslint).

### Concurrent checks (Erik, 15:55)

Erik asked whether the three pre-push checks could run concurrently, and whether an agent could pick a subset. Added `npm run check` (`scripts/check.ts`, `concurrently`; typecheck, unit and eslint in about 30 s instead of one after another; `npm run check -- typecheck unit` for a subset) on master b0cc243; the playbook's verification budget and the implement skill now prescribe it. Playbook change, logged here as the playbook requires.

### #73 one zip per Quiz

Dispatched 12:27 in `../Pubquiz-wt-3b` (worktree recreated on `ticket-73-zip-per-quiz`; the wp-env instance survived). Runs in parallel with #71; the two touch different files except `docs/walkthrough-customer-journey.md` and `docs/runbook-local-loop.md`, resolved at merge.

PR #77 open 13:09 (42 min; the empirical loop took most of it at 15 s per WP-CLI call). Interface gaps the implementer found empirically: (1) `sequence` restarts per line item, so the brief's `sequence + 1` gave two Quizzes of one order the same zip name; replaced by an order-wide index on both the TypeScript and PHP side; (2) the `deliverables` bucket only allowed PDF and MP3 MIME types, so a new migration adds `application/zip`. Reviews dispatched 13:12. Migration number clash: both #76 and #77 add `00010_*`; #77 merges first, #76 renumbers to `00011`.

Standards review 13:20: HARD worker README import list misses `src/render`; HARD `src/scripts/README.md` stale (per-file re-upload, output list); HARD the migration number clash (orchestrator's merge step); JUDGEMENT the bucket migration replaces the MIME array instead of appending; JUDGEMENT PHP-side zip numbering relies on WooCommerce item order, unpinned. Orthogonality, failure ordering, pruning and the fixture pin verified clean. Fix round sent immediately; pushed 13:29 (774d2f8).

Spec review 13:47: unit, storage, docs, Mijn account (through `WC()->customer->get_downloadable_products()`, the brief had named the wrong function) and the two-quiz mail PASS; HARD the zip number is computed twice (route from Supabase rows, PHP from WooCommerce items) and diverged on a stale row (mail `-1`, download `-2`); JUDGEMENT the about:blank story is unproven in a browser. Fix round 2 sent 13:50: one order-wide number computed in TypeScript and carried in the meta key, PHP parses it. Second fix round: the brief's `sequence + 1` formula was wrong and the implementer's replacement duplicated logic across languages; the brief should have fixed where the number is born.

Fix round 2 pushed 14:17 (be6a1de): `orderWideQuizSequence` in the domain, the deliverer bakes it into `pubquiz_download_<n>`, PHP parses the key, fixture pin that the PHP never recomputes; fresh two-quiz order #26 shows mail names equal to served filenames. Merged 14:19 (PR #77, #73 closed). Wall clock 112 min from dispatch, two fix rounds.

## Drift log

- 11:33 #70 implementer ran `find /` over the whole filesystem looking for Storefront's hook file; it hit the 120 s timeout and was backgrounded. The agent had already moved on to a scoped PowerShell search under `~/.wp-env`. Message: ignore the background task, keep searches scoped.
- 11:38 #70 implementer ran `npx supabase status` unfiltered, printing the local stack's keys into its transcript (brief: never print keys). Message: check the stack with `status -o env | grep -c API_URL`, never paste key values. Same class as #66; a brief rule for the next tickets.
- 13:05 #73 implementer applied its new bucket migration to the shared stack with `supabase migration up --local`. The brief forbade start, stop and reset but did not name `migration up`; the change is additive (one MIME type) and nothing else was in flight on the stack. No message (the agent had finished); brief rule for the next tickets: schema changes are applied by the orchestrator only.
- 14:03 #73 implementer (fix round 2) announced it would "clean up" a stale `orders` row on the shared stack (Erik's own order 24 from the morning instance, colliding on `woo_order_id` because the wp-env counter restarted). It had not deleted anything when checked; message: never delete rows on the shared stack, verify with fresh orders.
- 15:01 #72 implementer's order-status wait loop (WP-CLI at 14 s per call) ran without a timeout, hit the 180 s default and was backgrounded; it then filled the gap with an `echo waiting` call. Message: one foreground call with a 600 s timeout, poll `quizzes.status` through psql (1 s per call) and read the WooCommerce status once at the end.
- 16:03 #74 implementer ran `cat .env.local | Select-String -Pattern "x"` in Bash to check the file (Select-String is PowerShell, so nothing printed, by luck). Message: never read `.env.local` in any form, use a count; scan the captured `loop:up` output for key names before printing it; wait on port 3000 with one foreground loop if `loop:up` outran its timeout.

## Observations

- The review clone's `git clean -fdx -e node_modules` wipes its `.env.local`, so a Spec reviewer that starts its own worker there has no WooCommerce keys: the #77 reviewer's first order sat `pending` with three failed pg-boss attempts until it copied the worktree's `.env.local` (13:23, key names only printed). Brief rule for reviewers that run the loop: copy the worktree's `.env.local` first, or exclude it from the clean.

## Wave table

| Ticket | PR | Dispatch | PR open | Merged | Reviews HARD | Fix rounds |
|---|---|---|---|---|---|---|
| #70 | #75 | 11:20 | 11:57 | 12:22 | 1 (post 1 trashed on existence alone) | 1 |
| #73 | #77 | 12:27 | 13:09 | 14:19 | 4 (worker README imports, scripts README stale, migration number clash, zip number computed twice) | 2 |
| #71 | #76 | 12:12 | 12:39 | 14:40 | 2 (no red evidence in PR body, stale mode comment) | 1 |
| #66+#67 | #79 | 15:25 | 15:33 | 15:40 | 2 (dead JSON.parse, no red evidence in PR body) | 1 |
| #72 | #78 | 14:52 | 15:28 | 15:49 | 0 (4 JUDGEMENT: cap literal, required assertion, wording, pricing_type) | 1 |
| #74 | #81 | 15:59 | 16:19 | 16:41 | 2 (runbook `-o quiz.zip`, README "Soort quiz"; both outside the PR's own diff) | 1 |

## Wave end

- 16:45 the worktree's app stopped (pid file, port 3000 closed) so no worker could consume suite jobs; `npm run test:integration` on master 8f2856d: 11 files, 72/72 in 153 s. Then `loop:down` from the worktree (app, shop, Mailpit, cron ticker, Supabase stack), worktree `../Pubquiz-wt-3b` removed. Master HEAD 8f2856d plus this logbook.
- Spec 3b tickets #70, #71, #72, #73, #74 and the split-out #66/#67 are all merged; #69 and #55 stay open until Erik's second walkthrough.
- Stale `~/.wp-env` instance dirs from earlier tickets (37, 39, 41, 43, 56, shop, review, main-checkout 670d5ccc) left for Erik: each holds a WordPress install; removing them is his call.

### Observations for the retro

- Docs-only tickets still needed two reviews: the empirical Spec reviewer found both HARDs in files the PR did not touch, by grepping the scope the brief named. Keep "consistency of neighbouring docs" as an explicit acceptance check with a grep list; implementers treat untouched files as out of scope.
- Fresh-loop proof by the implementer plus a second fresh-loop replay by the reviewer cost about 8 minutes each; both found something (the implementer two wrong mail subjects, the reviewer the runbook filename). Worth it for a document Erik follows literally.
- Brief rules that stuck this wave: never read `.env.local` in any form; count key names in captured output before printing it; the orchestrator alone applies migrations and deletes rows; one foreground wait loop with a 600 s timeout, never echo polling. Two agents still tried to read `.env.local`; the implement skill should carry the rule, not only the brief.
- `npm run check` (Erik, 16:00) replaced three serial commands; every check line after it reported in about 30 s.
