# Spec 2 wave 2: follow-ups #51 and #52

Small wave after the wave 1 retro (`docs/logbook/2026-09-06-spec2-wave-1.md`). First wave under the trimmed verification budget: implementers never run the integration suite or `db:reset`; the Spec reviewer runs the suite once per branch; the orchestrator once on master at the end. No Standards reviewer for tickets without new behaviour. Implementers dispatched with the rewritten `implement` skill plus ticket facts.

## #51 scope integration suite cleanups (PR #53)

- Dispatched 12:36, PR opened 13:02, merged 15:16. One fix round.
- Implementer: shared `src/test-support/scoped-cleanup.ts` (billing emails, Quiz ids; deletes quizzes, orders, compositions, bucket objects in FK order) with its own red-first integration test; eight suites migrated; three table-wide `quizzes` assertions in the webhook suite scoped to the Woo order under test; PR #50 warning paragraphs removed from the runbook and playbook. Sentinel proof (foreign Order/Quiz/Composition/bucket object survive a full run) done once before push, as the brief allowed.
- Implementer reported 70/72 with the two pg-boss retry-policy tests timing out, "pre-existing on master". The Spec reviewer reproduced the timeouts on branch and master, and in a full run also got a third failure: an FK violation on `quizzes_composition_id_fkey` in the startup-sweep test, from in-flight retries of the timed-out tests running into later cleanup.
- Root cause (orchestrator, on the running stack): pg-boss 12 `createQueue` is a no-op for an existing queue. `pgboss.queue` showed `quiz-generation` created at 10:44 with production defaults (retry delay 5 s, backoff on), so the retry suite's `createQuizQueue(boss, { retryDelay: 1, retryBackoff: false })` never applied and the retries took 5+10+20 s. Master was green yesterday only because that run started from a fresh reset where this file created the queue first. Fix (efce06b): `createQuizQueue` calls `updateQueue` after `createQueue`, with a red-first `src/worker/boss.integration.test.ts` (`expected 5 to be 1`). Re-verified 73/73.
- Reviewer drift: after the retry investigation it spent about 20 minutes trying to delete leftover test rows and pg-boss jobs on the shared stack, retrying `DELETE`s the permission classifier blocked, then a tsx workaround Erik denied. Erik saw the prompts before the orchestrator did. One corrective message put it back on plan. Led to the 30-minute check-in rule (below).
- Other reviewer observations: a second Composition under the sentinel email appeared mid-run (created 13:01:42 UTC). Unexplained; nothing in the branch or master hardcodes that email. The sentinel rows themselves were untouched. Leftover `worker-sweep-idempotent-...` order and stuck pg-boss jobs on the stack were the reviewer's own investigation artifacts (master's unscoped test file has no `afterEach`).

## Process changes this wave

- **30-minute check-in** (`docs/agents/orchestration.md`): agents running past 30 minutes get peeked at with `scripts/agents/peek.ts` (prints the last tool calls and results from the agent's transcript; the task output files are usually empty) and one corrective message if off plan. Scheduled with a session cron at the first dispatch of a wave. Erik asked for this after watching the reviewer's permission prompts.
- Verification trial held: the implementer's single sentinel run plus the reviewer's two full runs were the only integration runs for #51. The fix round ran only the two affected files.

## #52 seed headroom

- Decision recorded on the issue: seed carries 2x ITEMS_PER_SLOT picture and music Items per Category and Difficulty (`per_cell` 1 to 2; 2640 Items). Dispatched 15:20 to `../Pubquiz-wt-52`; the brief allows at most two `db:reset` runs because a seed change cannot be verified otherwise.
