# Logbook: spec #1, wave 3 (2026-09-05)

Purpose: a factual record of how each orchestration session went, so the process can be evaluated at the end of the spec. One file per session or wave. Record what was dispatched, what review found, what was sent back, and friction. Keep opinions in the "Observations" section so the facts stay separable from the judgement.

## Session summary

- Orchestrator: main session (Fable). Implementer and reviewers: Sonnet subagents.
- Ticket: #11 dev script (end-to-end), the last ticket of spec #1. One branch, one worktree (`../Pubquiz-wt-11`). This ticket is the only one that needs the local Supabase stack, so the implementer and later the Spec reviewer run it sequentially, never together.
- Pre-dispatch probe: `tsx` could not load `@react-pdf/renderer` from a plain script (`ERR_PACKAGE_PATH_NOT_EXPORTED` on `@react-pdf/hyphenate/en-us`, because the CommonJS resolution path is used when package.json has no `type`). Fixed on master by the orchestrator (abb8def: `"type": "module"`, `npm run generate` placeholder, `import.meta.dirname` in the announcements script); typecheck, 49 unit tests and eslint unchanged. Without this probe the implementer would have discovered it mid-ticket.
- Dispatched at 20:42 with a fixed CLI, fixed file layout, three tdd seams (arg parsing, content assembly with injected downloads, end-to-end integration test including a real CLI spawn) and seed-derived expectations for the unsatisfiable case.

## Per ticket

(filled in when the PR lands)

## Friction log

## Observations (judgement)
