# 2026-09-17 spec 6 (storefront design, #142) wave 1

## Dispatch

- Loop up from the main checkout (Supabase, shop 45330, Mailpit 45332, ticker, app 3000). Product #10.
- Frontier: #143 (child theme foundation) and #148 (withdrawal waiver) in parallel, both Sonnet with the `implement` skill.
- Worktrees `../Pubquiz-wt-143` (`ticket-143-theme-foundation`) and `../Pubquiz-wt-148` (`ticket-148-waiver`) from master a427022, `npm ci`, `.env.local` copied. Briefs under `%LOCALAPPDATA%\Temp\pubquiz-briefs\143.md` and `148.md`.
- Shared-shop rules in the briefs: both edit in the worktree and copy across into the main checkout's `shop/`; copies stay in place until merge (the orchestrator restores before `git pull`). #143 alone may run `wp-env start` once from the main checkout, for the new theme mapping; #148 retries a refused curl once. Neither runs `setup-shop.php` or `shop:up`: the bootstrap rotates the REST key and would break the running app's delivery. The double `shop:up` idempotency check runs at merge, by the orchestrator.
- Check-in cron every 15 minutes (`4,19,34,49`).

## Drift log

- #147 implementer (11:02): a Playwright debug script overran its 40 s tool timeout into the background and the agent waited on it with `tail -f`, then kept writing debug scripts (seven) to find a coupon-hiding selector. Brief: one bounded foreground command with a large timeout, never wait on a background job. Fix: one message, 120 s foreground timeout, one CSS rule for the coupon and move on.

## Merges

- **PR 151 (#148 withdrawal waiver) merged** as 6e942a6. Implementer 12 min; red on the unfixed shop (order 39 accepted without the field), green orders 40 (implementer) and 41 (Spec reviewer) with meta, thank-you line, processing mail line, completed mail untouched. Standards: two JUDGEMENT notes, batched for a later fixes PR: pass `'site'` explicitly to `wc_format_datetime`; the brief named checkout-meta as the source of the cart rule but the cart-time rule lives in the feasibility plugin (the implementer reused the right one, should have flagged it under interface gaps). Master `npm run check` green (unit 361). Copied plugin removed from the main checkout before `git pull` (identical to the branch). Worktree removed.
- **PR 152 (#143 child theme foundation) merged** as 31aaf0b. Implementer 33 min plus one fix round (10 min). Red: no theme, Google Fonts link present, Storefront active. Both reviewers verified live: theme active with Storefront parent, all pages carry wordmark, cart count, account link, footer identity and five links, no third-party hosts, five woff2 200 with OFL files, ink/background contrast 16.65:1, focus ring, chrome plugin untouched. Fix round 1 (HARD from both reviews and the orchestrator's screenshot read): header rendered as two rows because Storefront closes `.col-full` at priority 41 and the chrome plugin had removed the navigation wrapper that reopened it; the theme now owns its own container (0 and 65) and a flex row; footer mailto escaping `esc_url`/`esc_html`. Wordmark SVG ships with an embedded base64 font instead of paths (Archivo is a variable font; opentype.js could not instance weight 900). Master `npm run check` green. Main checkout copies restored before `git pull`; `loop:down`/`loop:up` re-ran the bootstrap, theme stayed active (idempotency). Worktree removed.

- PR 156 (#146) merged 5f29162. Spec reviewer: curl add-to-cart with en/hard/Sport+Muziek 302 to the checkout and the cart shows Engels, Moeilijk, Sport, Muziek; Playwright mouse pass posts the same values; keyboard pass (real Tab, ArrowRight) moves the selects and wraps like native radios; served HTML keeps labels, selects and the checkbox fieldset; diff within the owned files. Standards: approve. Master `npm run check` 361/361 (the one timeout the implementer saw did not recur). Worktree removed. Copies of #146's files in the main checkout restored from master; #149's base.css copy left in place.

## Second frontier

- Dispatched #144 (bootstrap: legal pages, Voorbeeld, 19,95 incl. btw, mail branding), #145 (landing page as front page), #147 (straight to checkout, checkout and cart look) in parallel, worktrees `../Pubquiz-wt-144/145/147` from 31aaf0b. File ownership pinned in the briefs so copy-across into the main checkout's `shop/` cannot clobber: #144 owns `setup-shop.php`, theme `functions.php`, `inc/identity.php`; #145 owns `woocommerce/archive-product.php`, `landing.css`, `inc/seo.php`, `pubquiz-front-page-redirects.php`; #147 owns `base.css` and `pubquiz-checkout-redirect.php`. #144 proves bootstrap steps with a throwaway `tmp-144.php` eval-file run twice (never `setup-shop.php`, which rotates the REST key).
- **PR 154 (#145 landing page) merged** as ec7ec9d. Implementer 14 min plus one fix round (double hyphens in three copy strings and the meta description, no visited colour on the Voorbeeld link, `global $product` not restored). Spec reviewer verified live: section order, six FAQ entries, form with the three field groups and the cap message (the dropdown plugin is gated on `is_product()`, so the template carries a `$wp_query` bridge), 301s only for the product and Winkel URLs, SEO tags, same-origin hosts, order 52 with the `pubquiz_*` keys. Precedent noted by Standards: `inc/seo.php` puts `wp_head` behaviour in the theme; sanctioned for this one file, do not generalise.
- **PR 153 (#144 bootstrap pages, price, tax, mail branding) merged** as 396002b. Both reviews: merge. Red was the fixture test asserting the old price string. Verified live: six pages 200 with "Tekst volgt.", privacy page option set, no terms page, one NL 21% rate, 19,95 incl. btw on the front page and checkout, processing mail with wordmark, accent, footer identity. Idempotency proved with a throwaway eval-file (the bootstrap itself rotates the REST key, so nobody ran it against the live app).
- **PR 155 (#147 straight to checkout, checkout and cart look) merged** as 32074e7. Both reviews: merge. Verified live: add-to-cart 302 to `/afrekenen/`, three billing fields, "Nog een quiz toevoegen" link, order 53. Note from the Spec reviewer: the real add-to-cart posts to `/product/pubquiz/` with hidden fields, not to `/`; later briefs corrected.
- Master `npm run check` green after each merge. Worktrees 144, 145, 147 removed.

## Third frontier

- Dispatched #146 (configurator controls) and #149 (order-received, My Account, login look; mails checked) from 32074e7. Interface gap found while briefing #146: Taal and Moeilijkheid are `<select>` fields from the product-fields plugin, not radios as the ticket says; the brief pins a progressive-enhancement toggle and segmented control in theme JS over the selects, which stay the posted fields.
- #146 delivered PR 156 (943043c, merge 3c537b5): copies in the main checkout match the branch; Spec (empirical, review clone 2 on the branch) and Standards reviewers dispatched. Implementer notes: the seed has exactly 8 categories, equal to the cap, so the ninth-pick message cannot be triggered through the UI locally; the plugin's `<label>` has no `for`/`id` binding to its `<select>` (pre-existing, outside ownership). Unit run 360/361 with a 5 s timeout in `picture-handout-pdf.test.tsx` under parallel load, file untouched; to be checked against master at wave end.
- #149 delivered PR 157 (4093158): `base.css` copy in the main checkout matches the branch; Spec (review clone 1 on the branch) and Standards reviewers dispatched. Implementer fixed a Storefront specificity issue mid-ticket (download links underlined; `:link`/`:visited` in base.css). Its PR body says the footer legal links still 404; `/voorwaarden/` answers 200, so the reviewer verifies all five. Standards review of PR 156: approve; judgement finding segmented-control buttons 40 px from the prototype against the theme's 44 px rule (decided: 44 px), nit: no re-entry guard in `configurator.js`.

## Batched small fixes (one issue after the wave)

- Segmented-control buttons `min-height` 40 px from the prototype; make 44 px like the rest of the theme (landing.css). `:focus-visible` on the toggle/segment buttons computes to Storefront's purple 2 px ring, not the theme's 3 px accent ring; scope the theme ring to `#samenstellen [role="radio"]`. Add a re-entry guard to `configurator.js` `init()` (skip selects already carrying `.pubquiz-enhanced`).
- `pubquiz-withdrawal-waiver.php`: pass `'site'` explicitly to `wc_format_datetime` (Standards, PR 151).
- Decimal separator: prices render `€19.95` with a dot; set WooCommerce's decimal and thousand separators to Dutch (`,` and `.`) in the bootstrap (orchestrator, screenshots of PR 154 and 155).
- `setup-shop.php`: `is_wp_error` guard on the new `wp_insert_post` (Standards, PR 153).
- `base.css` checkout section: prefix the bare `#customer_details` / `#order_review` rules with `.woocommerce-checkout` (Standards, PR 155); the order summary card is half width at 1280, make it full width in the single column (orchestrator, screenshot).
- Checkout screenshots for the walkthrough were taken mid-AJAX refresh; retake after the fixes (Spec, PR 155).

