# 2026-09-17 spec 6 (storefront design, #142) wave 1

## Dispatch

- Loop up from the main checkout (Supabase, shop 45330, Mailpit 45332, ticker, app 3000). Product #10.
- Frontier: #143 (child theme foundation) and #148 (withdrawal waiver) in parallel, both Sonnet with the `implement` skill.
- Worktrees `../Pubquiz-wt-143` (`ticket-143-theme-foundation`) and `../Pubquiz-wt-148` (`ticket-148-waiver`) from master a427022, `npm ci`, `.env.local` copied. Briefs under `%LOCALAPPDATA%\Temp\pubquiz-briefs\143.md` and `148.md`.
- Shared-shop rules in the briefs: both edit in the worktree and copy across into the main checkout's `shop/`; copies stay in place until merge (the orchestrator restores before `git pull`). #143 alone may run `wp-env start` once from the main checkout, for the new theme mapping; #148 retries a refused curl once. Neither runs `setup-shop.php` or `shop:up`: the bootstrap rotates the REST key and would break the running app's delivery. The double `shop:up` idempotency check runs at merge, by the orchestrator.
- Check-in cron every 15 minutes (`4,19,34,49`).

## Drift log

- #147 implementer (11:02): a Playwright debug script overran its 40 s tool timeout into the background and the agent waited on it with `tail -f`, then kept writing debug scripts (seven) to find a coupon-hiding selector. Brief: one bounded foreground command with a large timeout, never wait on a background job. Fix: one message, 120 s foreground timeout, one CSS rule for the coupon and move on.

## Merges

- **PR 151 (#148 withdrawal waiver) merged** as 6e942a6. Implementer 12 min; red on the unfixed shop (order 39 accepted without the field), green orders 40 (implementer) and 41 (Spec reviewer) with meta, thank-you line, processing mail line, completed mail untouched. Standards: two JUDGEMENT notes, batched for a later fixes PR: pass `'site'` explicitly to `wc_format_datetime`; the brief named checkout-meta as the source of the cart rule but the cart-time rule lives in the feasibility plugin (the implementer reused the right one, should have flagged it under interface gaps). Master `npm run check` green (unit 361). Copied plugin removed from the main checkout before `git pull` (identical to the branch). Worktree removed.
- **PR 152 (#143 child theme foundation) merged** as 31aaf0b. Implementer 33 min plus one fix round (10 min). Red: no theme, Google Fonts link present, Storefront active. Both reviewers verified live: theme active with Storefront parent, all pages carry wordmark, cart count, account link, footer identity and five links, no third-party hosts, five woff2 200 with OFL files, ink/background contrast 16.65:1, focus ring, chrome plugin untouched. Fix round 1 (HARD from both reviews and the orchestrator's screenshot read): header rendered as two rows because Storefront closes `.col-full` at priority 41 and the chrome plugin had removed the navigation wrapper that reopened it; the theme now owns its own container (0 and 65) and a flex row; footer mailto escaping `esc_url`/`esc_html`. Wordmark SVG ships with an embedded base64 font instead of paths (Archivo is a variable font; opentype.js could not instance weight 900). Master `npm run check` green. Main checkout copies restored before `git pull`; `loop:down`/`loop:up` re-ran the bootstrap, theme stayed active (idempotency). Worktree removed.

## Second frontier

- Dispatched #144 (bootstrap: legal pages, Voorbeeld, 19,95 incl. btw, mail branding), #145 (landing page as front page), #147 (straight to checkout, checkout and cart look) in parallel, worktrees `../Pubquiz-wt-144/145/147` from 31aaf0b. File ownership pinned in the briefs so copy-across into the main checkout's `shop/` cannot clobber: #144 owns `setup-shop.php`, theme `functions.php`, `inc/identity.php`; #145 owns `woocommerce/archive-product.php`, `landing.css`, `inc/seo.php`, `pubquiz-front-page-redirects.php`; #147 owns `base.css` and `pubquiz-checkout-redirect.php`. #144 proves bootstrap steps with a throwaway `tmp-144.php` eval-file run twice (never `setup-shop.php`, which rotates the REST key).

