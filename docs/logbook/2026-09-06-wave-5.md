# Logbook: sampler property tests and the bug they found (2026-09-06)

Purpose: a factual record of how each orchestration session went, so the process can be evaluated. Facts per ticket; judgement under "Observations".

## Session summary

- Erik asked whether property-based testing is useful here; the answer was "only for the sampler", and he said to do it. Ticket #32 filed, `fast-check` 4.9.0 pinned on master (b97ac6a) before dispatch.
- Erik left the asset-only reviewer question to the orchestrator: decided single empirical reviewer, playbook updated (8fc6a8c).
- Stack started by the orchestrator only when #34 turned out to need a seed change; stopped after the merge.

## Per ticket

### #32 Sampler property tests (PR #33) — dispatched 16:54, merged 17:37 (43 minutes)

- Implementer: one commit (f25b1a8), 24 minutes. Two property files, brute-force oracles for the matching and for slot shortfall, three mutation counterexamples quoted. Reported a real bug found by property 1: in `single_category` mode `sampleComposition` never excludes Items placed in earlier slots, so two slots can hold the same Items. Scoped that check to `mixed` mode and reported rather than fixed, per the brief. Filed as #34 by the orchestrator.
- Standards review (HARD): red evidence missing for property 3 and the sampler half of property 4; properties 1-3 return early on sampler failure and the success rate was never measured, so a sampler that always fails would pass. Spec review: all criteria PASS; reproduced all three mutations; confirmed #34 on master's code with a throwaway script (68 unique ids out of 80 across slots).
- Fix round (16e2b30): two more mutations quoted (Math.random in the shuffle; forced shortfall). The success count of the original generator was 5 in 100 runs, near vacuous; widened to 14-20 Subsubcategories with weighted density, now 70 in 100, and each property asserts at least 50 successes. README points to #34. Property test time about 0.6 s.
- Merged; master checks green (typecheck, unit 87/87, eslint).

### #34 single_category duplicate Items across slots (PR #35) — dispatched 17:40, merged 18:12 (32 minutes)

- Implementer (same agent as #32, in its worktree): two commits (b5c6c27, de3fe37). `sampleComposition` now carries a `placedItemIds` set (a copy of the caller's `excludedItemIds`, grown after each successful slot). Red evidence: unit repro `expected 30 to be 80`; property 1 without its `mixed`-only guard `expected 31 to be 80`. The property generator was widened again (25-35 Subsubcategories, 3-10 Items per cell) because honest exclusion dropped successes to 9 in 100; now 83.
- Scope extension: the fix broke the seed's implicit assumption. `single_category` needs 60 distinct text Items per Category and Difficulty (6 Text Rounds x 10 Subsubcategories); the seed had 30. Seed density raised to 7 text Items per Subsubcategory and Difficulty (2160 Items: 1680 text, 240 picture, 240 music), header rewritten with the invariants, `supabase/README.md` "Pool coverage" and CONTEXT.md line 33 updated, integration counts 717 to 2157, and the "unsatisfiable requests" integration test redesigned around single_category (second Quiz on the same Category hits a genuine shortfall). The orchestrator's guess that two other integration tests would break was wrong; the implementer checked and corrected it.
- Standards review: no HARD findings. Confirmed the set is built before each slot, a failing slot never mutates it, the caller's set is never aliased, and `mixed` mode cannot change because its slots have distinct Categories. Every fixture change traced to the new rule; one test legitimately moved to `mixed` mode. Arithmetic of the seed README checked against seed.sql. JUDGEMENT: fact-bearing rows now follow a fixed phase per cell (cosmetic); the property success floor of 50 has more slack than it enforces.
- Spec review: typecheck ok, unit 88/88, eslint ok, integration 15/15. Red evidence reproduced against master's `index.ts`. Real runs on the new seed: one single_category easy Quiz on Sport succeeded; the second on the same email failed honestly with "slot 0, Category Sport, shortfall 1" (one of the seed's deliberately nl-less Items sits in Sport easy, so that Subsubcategory has 6 instead of 7); a mixed Quiz succeeded. Database counts match the README. Off-ticket: the redesigned integration test no longer exercises a `mixed`-mode shortfall end to end (sampler unit tests still cover it in both modes).
- No fix round. Merged b9ba958; master checks green; worktree `../Pubquiz-wt-32` removed; stack stopped.

## Wave result

| Ticket | PR | Dispatch | Merged | Fix rounds |
|---|---|---|---|---|
| #32 | #33 | 16:54 | 17:37 | 1 |
| #34 | #35 | 17:40 | 18:12 | 0 |

## Friction log

- The Spec reviewer's persistent clone had a stale local `master`; `git diff master...` showed unrelated files until it used `origin/master`. Playbook amended (bf820fe) to say `origin/master`.
- #34 grew from a ten-line sampler fix into a seed change with five test files touched, because the seed density was never derived from what each mode consumes. The invariants are now written in the seed header, which is where the next person will look.

## Observations (judgement)

- The property found a genuine defect in the core engine on its first run, which is the whole argument for the ticket. The Standards question about vacuous success was the most valuable review finding of the day: 5 successes in 100 would have made the properties decorative.
- Reusing the #32 implementer for #34 saved the worktree and context setup; it already knew the property files and corrected the orchestrator's wrong prediction about which integration tests would break. Worth doing whenever a follow-up ticket lands on the same module within the session.
- The mixed-mode shortfall integration test that #34 repurposed is a small coverage gap. Not worth a fix round; a candidate for the next batched fixes PR.
